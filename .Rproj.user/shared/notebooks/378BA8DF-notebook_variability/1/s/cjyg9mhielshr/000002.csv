"0","```r
# Import column data
column_vars <- read.csv(file = \"./output/column_vars_2017-10-09.csv\")
# Select only good orientation & quality
column_vars <- subset(column_vars, orientation == \"Good\" & quality == 1)
# Assign photic zone parameter for stations where bottom light levels > 1% downwelling irradiance
column_vars$photic_zone <- column_vars$z_od1
column_vars$photic_zone[is.na(column_vars$photic_zone)] <- column_vars$bottomdepth[is.na(column_vars$photic_zone)]
base_map <- map_data('world')
base_map <- base_map[which(base_map$subregion == \"Alaska\"),]
cruises <- sort(unique(column_vars$cruise))
```"
